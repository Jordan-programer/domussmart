spring.application.name=domussmart

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:aws-0-eu-west-1.pooler.supabase.com}:${DB_PORT:5432}/${DB_NAME:postgres}
spring.datasource.username=${DB_USERNAME:postgres.zvxacteddpjymwqyotbf}
spring.datasource.password=${DB_PASSWORD:oP4h0frDVBD2UBdS}

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Security (JWT) - Placeholders for now
jwt.secret=${JWT_SECRET:secret}
jwt.expiration=${JWT_EXPIRATION:86400000}
